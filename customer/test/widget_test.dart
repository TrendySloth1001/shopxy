// Placeholder widget test. The default counter test from `flutter create`
// was removed because this app has no MyApp/counter widget; full coverage
// will be added alongside the auth + shops features.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
